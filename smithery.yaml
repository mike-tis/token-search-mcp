# Smithery Configuration File
runtime: "typescript"
env:
  NODE_ENV: "production"
  
startCommand:
  type: "http"
  configSchema:
    type: "object"
    properties: {}
  
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
